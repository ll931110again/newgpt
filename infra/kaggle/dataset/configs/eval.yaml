run_name: eval_default

eval:
  tasks:
    - arc_easy
    - hellaswag
  batch_size: 1
  limit: 100

model:
  model_name_or_path: null
  tokenizer_name_or_path: null

logging:
  output_dir: runs/eval

